{
  "name": "@tracegate/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test *.test.ts",
    "test:e2e:local": "node --test functional-fakes.test.ts"
  },
  "dependencies": {
    "@tracegate/evaluation": "workspace:*",
    "@tracegate/grading": "workspace:*",
    "@tracegate/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "26.4.0",
    "typescript": "7.0.2"
  }
}
